<resources>
    <!-- Matches the app's color-bg token / theme_color in manifest.json, so
         the launch window, status bar and nav bar don't flash a different
         colour before the WebView paints. -->
    <color name="rise_cream">#FAF9F5</color>
</resources>
