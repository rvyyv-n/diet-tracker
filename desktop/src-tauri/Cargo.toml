[package]
name = "rise"
version = "1.0.0"
description = "Rise: Diet Tracker — desktop shell"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2" }

[[bin]]
name = "rise"
path = "src/main.rs"
