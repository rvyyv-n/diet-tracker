{
  "name": "rise-reminders",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that pings Rise clients at each meal block's nominal time.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "keys": "node tools/gen-vapid.mjs",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
